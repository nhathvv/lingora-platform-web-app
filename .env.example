# Copy to .env.local — NEVER commit .env.local
# NEXT_PUBLIC_ vars are exposed to the browser; all others are server-only.

# ── API ───────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
                                         # <required> base URL of lingora-platform-api
